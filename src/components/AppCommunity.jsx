import SectionTitle from "./SectionTitle.jsx";

export default function AppCommunity() {
  return (
    <section id="community" className="pt-40 pb-28 md:pb-40">
      <SectionTitle
        hasImage={true}
        text="Happy clients"
      />
    </section>
  )
}